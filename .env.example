# =============================================================================
# MONAD NODE CONFIGURATION
# =============================================================================
# CRITICAL: Monad uses ports 8080 (HTTP) and 8081 (WS), NOT 8545/8546!

# ----- LOCAL NODE (RECOMMENDED for production) -----
# Using a local node enables aggressive optimizations:
# - 100ms polling interval (vs 1000ms for remote)
# - 5ms receipt polling (vs 100ms for remote)
# - Larger multicall batches (100 vs 50)
# - Tighter gas buffer (10% vs 15%)
MONAD_RPC_URL=http://127.0.0.1:8080
MONAD_WS_URL=ws://127.0.0.1:8081

# ----- REMOTE RPC (alternative) -----
# Uncomment below and comment above to use a remote RPC
# MONAD_RPC_URL=https://monad-mainnet.g.alchemy.com/v2/YOUR_ALCHEMY_KEY
# MONAD_WS_URL=wss://monad-mainnet.g.alchemy.com/v2/YOUR_ALCHEMY_KEY

# =============================================================================
# WALLET CONFIGURATION
# =============================================================================
# Required for trade execution (not needed for monitoring only)

# Your wallet private key (without 0x prefix)
PRIVATE_KEY=your_private_key_without_0x_prefix

# Optional: Wallet address (derived from private key if not set)
# WALLET_ADDRESS=0xYourWalletAddressHere
